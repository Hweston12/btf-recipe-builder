# Server-side only — read exclusively by src/lib/recipeEngine/claudeRecipeEngine.ts via the
# Anthropic SDK's own credential resolution. Never exposed to client code (CLAUDE.md non-negotiable).
# Get a key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
