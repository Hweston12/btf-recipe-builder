{
  "name": "@btf-recipe-builder/calculation",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic calculation module for the BTF Recipe Builder (volume/density reconciliation, water top-up, IDDSI flow test interpretation).",
  "main": "index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^3.0.0"
  }
}
